{
  "name": "@caistech/usage-meter",
  "version": "0.3.0",
  "description": "Per-product / per-API usage meter — Anthropic/OpenAI/OpenRouter adapters plus call-grain telemetry (observeAiCall). No-op until env-configured.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "files": [
    "dist/"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/caistech/cais-shared-services.git",
    "directory": "packages/usage-meter"
  },
  "license": "PRIVATE"
}
