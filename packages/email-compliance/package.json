{
  "name": "@caistech/email-compliance",
  "version": "0.3.0",
  "description": "Australian Spam Act 2003 email compliance — sender identity + consent-basis footer, assertCompliant/assertJurisdictionAllowed guards, AND the runtime opt-out layer: signed unsubscribe links, a framework-agnostic unsubscribe route, List-Unsubscribe headers, and a durable suppression store the send path must consult.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "test": "vitest run"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "files": [
    "dist/",
    "migration.sql",
    "README.md"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/caistech/cais-shared-services.git",
    "directory": "packages/email-compliance"
  },
  "license": "PRIVATE",
  "type": "module",
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
