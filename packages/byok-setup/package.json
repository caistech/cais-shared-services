{
  "name": "@caistech/byok-setup",
  "version": "0.1.0",
  "description": "BYOK key-onboarding wizard: read a product's byok.config.json, validate pasted keys, generate secrets, and distribute values to .env.local + Vercel. CLI now; React wizard later (shared core).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "bin": {
    "byok-setup": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "@types/node": "^20.0.0"
  },
  "files": [
    "dist/"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/caistech/cais-shared-services.git",
    "directory": "packages/byok-setup"
  },
  "license": "PRIVATE"
}
