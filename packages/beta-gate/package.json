{
  "name": "@caistech/beta-gate",
  "version": "0.2.0",
  "description": "Reusable trial-clock (N-day, extendable) + usage caps — per-day/total COUNTS and a $-denominated cost cap, with a soft-warn band (pctUsed/warn) so products warn before the ceiling. Supabase-injected, framework-agnostic.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "migration.sql",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "@supabase/supabase-js": ">=2.0.0",
    "typescript": ">=5.0.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/caistech/cais-shared-services.git",
    "directory": "packages/beta-gate"
  }
}
