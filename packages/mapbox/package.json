{
  "name": "@caistech/mapbox",
  "version": "0.2.0",
  "description": "Mapbox Geocoding API v5 wrapper — forward/reverse search, coordinate parsing, static map URLs (incl. satellite styles). Australia-biased defaults.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  },
  "files": [
    "dist/"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/caistech/cais-shared-services.git",
    "directory": "packages/mapbox"
  },
  "license": "PRIVATE",
  "type": "module"
}
