{
  "name": "@caistech/mapbox",
  "version": "0.1.0",
  "description": "Mapbox Geocoding API v5 wrapper — forward/reverse search, coordinate parsing, static map URLs. Australia-biased defaults.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "files": ["src/"],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "license": "PRIVATE"
}
