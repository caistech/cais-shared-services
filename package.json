{
  "name": "cais-shared-services",
  "version": "0.1.0",
  "private": true,
  "description": "Shared services for Corporate AI Solutions projects",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dennissolver/cais-shared-services.git"
  },
  "license": "PRIVATE"
}
